# Changelog

## 2.5.14-community.64 — 2026-09-22
- 把"组 × addonlist 生命周期"行为接到界面上：
  - 组归属新增 `missing` 标记，并新增 `GetModStrategyGroupMissingMembers`：分组菜单显示
    「组名（N） · 含 M 个缺失」，设置页策略组条目下方用黄字提示
    「⚠️ 文件缺失 M 个：…（放回同名文件会自动回到组里）」；
  - 删除 Mod 前若它属于策略组，先弹提示「该 Mod 还在 N 个策略组里：…；删除后组成员会保留
    （显示为缺失成员），把同名文件放回来就会自动重新回到组里」；
  - 隐藏 / 显示 Mod（加去 `_` 前缀改名）同样触发键迁移，避免一隐藏就掉组。
- 明确并加固"策略组 × addonlist 生命周期"：
  - 改名 Mod 时**自动改绑**策略组 / 优先级分层 / 依赖 / 冲突忽略清单里的旧键（此前会留下悬空记录）；
  - 应用策略组时**跳过文件已不在列表里的成员**并报告（`ModStrategyGroupApplyResult.skipped`，
    提示语会说明"× 个成员的文件已不在列表里，已跳过"），不再往 addonlist 写幽灵条目；
  - 新增 3 项生命周期测试：成员在"整组关闭 + 手动删条目"后仍在组内、重新应用能写回；
    文件删除后跳过、放回后自动重新命中；改名后四类本地记录全部跟到新键。


- MOD 管理页新增「分组」菜单：按策略组筛选列表、整组开关（只改 `addonlist.txt` 的 0/1）、
  整组优先级前移 / 后移（只写 `priority.json`，仍需「按分层应用」才重排）、
  「用选中的 Mod 建组」。
- 列表与卡片显示所属策略组徽标（`组：<组名>`）；单击徽标即可整组开关，整组优先级移动在
  「分组」菜单里。组归属变化后徽标与筛选菜单立即刷新。
- 新增「分组建议」推导：只读扫描工坊合集成员、已扫描目录里的套件文件夹、同名文件的不同版本、
  主体识别（替换目标）、语音角色、共同标签（任意两个标签）、文件名前缀、同一作者，
  输出可勾选 / 可改名的候选组。
  排序遵循「内容证据优先」——`工坊合集 / 文件夹` → `主体 / 共同标签 / 同名变体` →
  `前缀 / 作者`，所以武器归武器、角色归角色（「M16 武器」只收替换 M16 的 Mod）。
  多信号命中额外加权，成员超 8 个后按 4 分/成员降权（工坊合集与文件夹不降权、上限放宽到 300）；
  占位符作者（`AUTHOR_NAME`）、多人联名、高产作者与跨主分类的候选都会被过滤；
  解析器标为"低置信度"的主体（例如「脚本资源（无法确认具体对象）」）不作为分组依据；
  主分类字段里的脏值（`三角洲`、`Milfy`）按"未知"处理，不会误判成跨分类而拆组；
  已整组建过的建议沉到列表末尾，避免占满 40 条上限后看不到新建议。
  推导不写任何文件，也不改动 `addonlist.txt`；`addons` 下其它子目录仍不参与扫描。
- 「分组建议」弹窗重做为冲突检测那种大界面（最宽 1180px / 最高 88vh）：每个成员一行，
  显示标题、文件名、位置、游戏开关与优先级徽标，并带 **详情 / 游戏开关 / 启用·禁用·复制到 addons**
  按钮（按位置决定可用动作），每条建议还支持「全选 / 全不选」后创建为组。
- 新增**外部组建议导入**（标准格式 v1）：可以把大模型 / 人工 / 外部脚本的推导结果写成
  `group_suggestions.json` 导入，程序负责校验成员键（支持 addonlist 键、裸文件名、绝对路径）、
  报告未匹配成员与重名、并把导入的建议排在内置启发式之前展示（带「导入建议」徽标）。
  配套「导出 Mod 清单…」可导出键、标题、作者、标签、主体、语音角色、所在文件夹等元数据
  供推导方分析；导入文件里的 `strategy` 会用于建组策略。文档见
  `docs/features/group-suggestion-import.md`。
- 新增**智能体提示词**：分组建议弹窗里点「准备给智能体的材料」自动导出 Mod 清单 +
  复制提示词（已填好清单路径与建议文件路径）；也可单独「复制智能体提示词」/「保存提示词…」。
  提示词随 EXE 发布，写明要读的字段、判断依据、标准输出格式、四种策略与硬性约束。
- **分组推导引擎重构为独立纯逻辑包 `internal/grouping`**（标准化 / 低耦合 / 高性能）：
  信号统一登记在 Catalog（ID + 展示名 + 可信度 + 是否用户维护结构），一次遍历建立
  目录 / 标签对 / 作者 / 语音角色 / 主体 / 前缀 / 文件名倒排索引，各信号只读索引；
  外部的工坊合集与建议文件通过"注入候选"进入同一条合并 / 排序 / 配额流水线；
  新增单信号配额（默认 20 条）与推导统计（`mods/candidates/merged/dropped`）；
  建议收件箱按 mtime+size 缓存，避免每次推导重复解析与重建键索引。
- **导出给智能体的 Mod 清单信息补全**（扫描时顺带统计，不额外读取 VPK）：
  每个 Mod 现在带 VPK 内部结构（顶层目录与条目数、压缩后的替换目标 `structure.targets`、
  ≤5 条代表性路径、条目数与体积）、`contentSubjects`、XDR 摘要与模型规模、战役名、
  位置 / 游戏内开关 / addonlist 顺序号 / 有效分层 / 优先级来源、文件大小与修改时间；
  清单顶层新增紧凑的 `clusterHints`（内置推导候选簇，约 8 KB）与预计算的
  `duplicateGroups`（同名同体积 / 同名不同位置，最多 400 组），并在 `notes` 里说明
  "先读 clusterHints 与 duplicateGroups、再按需查 mods 明细"的使用方式。
- 清单再补一层"LytVPK 已经解析好的全部信息"：`addonInfo.*`（VPK 内 `addoninfo.txt` 的版本 / 描述 /
  主页 / 更新标记 / 战役章节 / 模式）、`workshop.*`（本地 `.meta` 的工坊标题、作者、简介、标签、
  详情页、预览图、更新时间、"稍后再看"里的热度统计）、`management.*`（策略组、启用方案、
  已声明依赖、冲突忽略清单、已保存的工坊合集）以及顶层 `coverage`（各类信息可用条数）。
  智能体提示词同步补全字段清单——**无需自己打开 VPK 或访问工坊**，
  实测本机 1968 个 Mod：`withStructure=1968` / `withAddonInfo=1590` / `withWorkshopMeta=13`。
- 按外部智能体的实测反馈（`LytVPK-分组建议-实测反馈-20260922.md`）修正与增强：
  - 清单升到 **`version: 2`** 并新增 `schemaRev` / `capabilities`，不再出现"同版本字段完全不同"的情况；
  - 每条记录新增唯一 **`entryId`**（`root/…`、`disabled/…`、`workshop/…`）与 `relativePath`：
    同一个文件在根目录与 `disabled` 各有一份时也能分别寻址，`duplicateGroups` 不再出现重复键；
  - `location` / `folder` 改为从物理路径现算，消除"folder 与 location 互相矛盾"；
  - 建议文件的 `members` 支持 `entryId` / `disabled\…` / `workshop\…` / 绝对路径 / 裸文件名；
    解析顺序改为"entryId → 精确键 → 裸文件名"，**同一个键的多位置副本不再算歧义**
    （实测把该文件里 229 条假警告降为 0）；
  - 新增顶层 `scope`（覆盖 addons 根目录 / workshop / disabled，并给出被排除的子目录与 VPK 数）、
    `ungroupedKeys`（内置推导未覆盖的键）、`themeHints`（跨槽位同主题套装候选）、
    `preloadHints`（疑似前置库）、`unreadableMods`（磁盘有但解析失败，例如 .vpk 实为 ZIP），
    `clusterHints` 上限从 60 提升到 600（实测 481 条），每条记录新增 `xdrSlots` 骨骼槽证据；
  - 新增**命令行 dry-run 校验** `--validate-group-suggestions <文件> [--out <json>]` 与
    `--export-grouping-catalog [路径]`：外部智能体可以"写文件 → 校验 → 修正"，结果含逐成员
    `resolved/matched/ambiguous` 与全部警告（不再只留前 3 条），无需打开界面。
- 导入建议的展示修正（导入智能体 195 组文件时发现）：
  - 保留**文件里的编排顺序**：外部建议的分数按文件位置递减（90 → 60），不再被"成员少优先"重排；
  - **不再只显示 40 条**：引擎层放宽到 240 条上限，应用层按"外部建议数 + 40 条内置"计算展示上限
    （实测 195 条导入时显示 234 条 = 194 条导入 + 40 条内置），避免"文件有 195 组却只看到 40 组"。
- 按外部智能体第二轮实测反馈（`LytVPK-分组建议-v2-说明-20260922.md`）修正：
  - **修掉 `clusterHints` 重复缺陷**：信号配额裁剪只改了本地切片长度、被删候选残留在尾部，
    导致「官方近战、武士刀」重复 395 次；现在裁剪返回新切片，并新增"结果不允许出现
    重复成员集合"的回归测试；
  - 导出侧改用"广度优先"配额（120/信号、上限 600）：实测 `clusterHints=340`（全部唯一）、
    覆盖 **1051/1968 个 Mod（53%，修复前 230/12%）**，未覆盖的 914 个键落在 `ungroupedKeys`；
  - `clusterHints` / `themeHints` / `duplicateGroups` 的成员新增 **`memberEntryIds` / `entryIds`**，
    root 与 disabled 同名副本也能精确寻址；`duplicateGroups` 按成员集合去重（400 → 229 组）；
  - 文档/提示词补上"首次 `--validate-group-suggestions` 会解析 VPK 建缓存、偏慢属正常"的说明。
- 修复 `LogError` 在没有 Wails 上下文时（启动早期、后台任务、自动化场景）直接中断进程的问题：
  现在只写日志，不再把"记录一个错误"变成崩溃。
- 未设置分层的 Mod 排序默认按优先级（有效分层升序，其次 `addonlist.txt` 顺序号）。
- 修复 Mod 管理工具栏在 1400×900 下按钮互相压住的问题：目录选择器不再被压成 0 宽度，
  工具栏允许换行，动作区不会撑破容器。
- 修复「分组」菜单只有 161px 宽、且向右溢出被 `.page-view` 裁剪导致点击穿透到侧边栏的问题
  （改为组合选择器 + 与按钮左对齐展开）。
- 策略组交互改用应用内弹窗：`window.prompt` 在 WebView2 下没有默认实现，
  `window.confirm` 是阻塞式原生对话框，两者在自动化 / 锁屏环境下都会让流程卡住或直接返回空值。

## 2.5.14-community.63 — 2026-09-22

- 修复设置页面直接报「设置页面加载失败：GetWorkshopAutoRedownload is not defined」的问题：设置页渲染时漏声明了 6 个后端绑定。
- 修复设置页新增控件（原版文件白名单、工坊合集、策略组权重、上级分组、自动重下）在运行时全部报 `deps.X is not a function` 的问题：绑定改为整体透传，删掉第二份手写的绑定清单。
- 补齐原版文件白名单相关注入：从原版 pak01 生成批次、刷新白名单状态、读取白名单状态。
- 修复「关于」页崩溃报告面板永远空白的问题：关于弹窗的渲染入口漏传崩溃报告绑定。
- 顺带修复因同一原因在运行时失效的三项既有功能：策略组自动联动、启用方案的自动化快照（策略组 + 依赖 + 分层）、体检报告导出。
- 崩溃报告面板在接口缺失时改为显式提示，不再静默留空；后端错误日志由 `Object` 改为可读的「类型 | 消息 | 文件」。
- 新增前端绑定注入契约测试（5 项）：裸用未声明绑定、`deps.X` 未注入、解构未注入、每个 `renderAboutPage` 调用点漏传参数、`bindSettingsPage` 必须整体透传，防止同类问题再次出现。

## 2.5.14-community.62 — 2026-09-22

- 新增「统一优先级模型」：Mod 可以设置可编辑的优先级分层，策略组可以设置组权重（同一 Mod 属于多个带权重组时取最小值）；冲突判定、覆盖关系与「按分层应用」的重排共用同一套有效分层。未设置分层时行为与以前逐字节一致，`addonlist.txt` 顺序仍是游戏侧唯一权威。
- 新增每个 Mod 自己的冲突忽略清单：与内置规则、设置页全局清单取并集，只让该 Mod 退出对应路径的判定，并在结果里标注「因 Mod 自身的忽略规则跳过」。
- 新增本地记录（启用方案 / 策略组 / 依赖 / 忽略清单 / 优先级）的备份轮转：最短间隔 15 分钟、内容与上一份相同则跳过、最多保留 10 份、溢出进回收站。
- 新增变更驱动的冲突自动复检：写入 addonlist.txt、应用方案 / 策略组、启用依赖、移动 / 重命名 / 删除 Mod 后自动失效并按需重算，列表显示冲突角标，并给出需要用户确认的修复建议（只写分层，不自动改文件）。
- 新增游戏原版文件白名单：默认启用内置高精度批次；可按类别从本机原版 `pak01_dir.vpk` 生成增量批次，删除对应文件即可停用；批次缺失或损坏时自动降级，不阻断冲突检测。
- 新增「下载失败后自动重下一次」（默认关闭）：新任务失败时自动重试一次，用户主动取消不重试，也可在下载队列里单独开关某个任务。
- 新增「工坊合集」实体化记录：保存合集的全部可下载成员，支持检查新增 / 下架成员、一键下载本地缺失成员；删除记录不会删除已下载的 Mod 文件。
- 策略组支持上级分组（树形层级，最多 4 层）：只影响分组展示，不改变优先级；默认仍为扁平列表，并会拒绝成环与超深设置，坏数据自动降级到顶层。
- 新增文案层（i18n）：抽出中文文案目录与运行时，工具箱页面已迁移，其余页面按同一约定逐页迁移。
- 新增自身崩溃上报：后台任务 panic、启动流程 panic 与前端未处理异常会写成只保存在本机的报告（版本、堆栈、日志尾部），关于页可直接打开报告目录；与已有的崩溃转储查看器互补，不联网上传。
- 修复设置页部分依赖绑定未转发，导致策略组权重、自动联动等控件在运行时不可用的问题。

## 2.5.14-community.61 — 2026-09-05

- 问题 VPK 不再因为缺失或损坏的根目录 `addoninfo.txt` 被硬拒绝，仍会按用户选择写入 `addonlist.txt`。
- 在启用、恢复、复制、移动、删除、重命名、隐藏和批量操作前增加显眼的 VPK 完整性风险提示；检测到问题时仍可继续，也可打开检测/修复工具。
- 完整性预检失败只显示警告，不会把检测故障变成新的操作阻断；新增相关 Go 与前端回归测试。

## 2.5.14-community.60 — 2026-09-05

- 修复 VPK 完整性工具“修复并另存为”忽略同名创意工坊 `.meta` 的问题：现在优先使用工坊标题，并仅补全缺失的 `addoninfo.txt` 标题、作者、描述和 Workshop URL 字段。
- 修复后的 VPK 会同步保留有效的 Workshop 元数据和同名预览图，改用 repaired 文件基名生成 sidecar；缺失或损坏的 `.meta` 会安全降级，不阻止 VPK 修复。
- 新增缺失 `addoninfo.txt`、Workshop 元数据保留、预览图复制、原字段优先和损坏 `.meta` 降级回归测试。

## 2.5.14-community.59 — 2026-09-04

- 修复设置页保存 `autoexec.cfg` 后刷新状态时引用未声明 `autoexecInfo` 导致保存提示失败的问题。
- 将 autoexec 编辑器的文件元数据状态收敛到独立控制器；保存成功但保存后刷新失败时会分别提示，避免把已写入文件误报为保存失败。

## 2.5.14-community.58 — 2026-09-03

- 新增批量转移创意工坊 Mod 至 addons 根目录：支持多选、右键菜单和批量操作下拉入口，并显示逐项进度与成功/失败/跳过汇总。
- 批量入口复用当前 Fork 的冲突处理、伴随文件复制、addonlist.txt 同步和缓存更新逻辑，保留 workshop 原件，不覆盖既有单项转移行为。

## 2.5.14-community.57 — 2026-09-03

- 修复加载顺序预览与应用会被 `addonlist.txt` 中同状态重复条目完全阻断的问题：现在会保留首次出现的位置和原始文件名，并在应用排序时一次性清理后续相同状态的重复项。
- 同一 Mod 若记录为相互矛盾的游戏内状态（`0`/`1`），仍会阻止排序，但错误会明确指出两条冲突记录的位置和状态，避免静默选择或误改开关。
- 加载顺序窗口读取当前条目时也会使用同一规范化逻辑，不再把同一个 Mod 显示为多个不可区分的规则目标。

## 2.5.14-community.56 — 2026-09-02

- 压缩包管理器的 VPK 对比范围现在严格限制为 `addons` 根目录、`workshop` 和 `disabled`；其他 addons 子目录中的 VPK 不会再误标为已导入。
- ZIP 内 VPK 遇到当前解码器不支持的压缩算法时，改为显示“压缩算法不支持”，明确说明文件树仍可见、VPK 内部目录未检查，并建议使用 7-Zip 解压后再检查，不再笼统显示为 VPK 读取失败。
- 新增路径范围和 ZIP 压缩算法错误分类回归测试。

## 2.5.14-community.55 — 2026-09-02

- 未记录的 Mod 不再默认直接写入“游戏内开启”：点击状态按钮会提供“游戏内关闭”“游戏内启用”“禁用”三个明确选项。
- “禁用”会复用现有文件管理流程，将根目录 Mod 移入 `disabled`；workshop 原件会提示需先复制到 addons，避免执行无效操作。
- 冲突分析中的“游戏开关”按钮同步使用上述选择流程，并补充状态提示与回归测试。

## 2.5.14-community.54 — 2026-09-01

- 修复压缩包管理搜索框在中文输入法组合输入期间被重绘的问题：现在会等待 `compositionend` 后再刷新，不会再把中文输入强制截断为英文拼音。
- 搜索防抖控制器独立测试覆盖 IME 组合输入和待执行英文搜索回调取消场景。

## 2.5.14-community.53 — 2026-09-01

- 压缩包管理器新增目录快照与文件签名缓存：未变化的压缩包直接复用结果，新增、删除或发生变化的文件才重新读取；7Z 密码重试仍保持单包增量刷新。
- 压缩包扫描使用共享协程池并发处理，`addons`/`addonlist.txt` 对比索引按目录签名缓存，减少重复遍历和重复解析。
- 搜索输入增加防抖，勾选压缩包只更新选择状态；文件树改为延迟展开和分段显示，新增紧凑/舒适显示密度与固定工具栏，保留完整文件树、VPK 对比、移动、定位和打开能力。

## 2.5.14-community.52 — 2026-09-01

- 压缩包 VPK 对比索引现在明确覆盖 `addons` 根目录、`disabled` 和 `workshop`，并展示实际位置及 addonlist.txt 游戏内开关状态，避免把已禁用或工坊 Mod 误显示为“未找到”。
- 新增压缩包名称/路径/VPK 名称搜索、状态筛选和名称/状态/大小排序；多列卡片流保持高密度浏览，搜索和排序只在当前会话数据上执行。
- 7Z 密码重试改为单包增量刷新，不再重新扫描整个目录；新增会话内压缩包快照与显式单包刷新接口，移动操作仍按文件系统变化需要刷新。

## 2.5.14-community.51 — 2026-09-01

- 修复压缩包内的大型 VPK 被 32 MiB 总体积上限误判为“VPK 读取失败”的问题：现在仅流式读取 VPK 目录元数据，并保留独立的 8 MiB 安全上限；真正超限时明确显示“目录读取受限”，不再直接判坏。
- 7Z 扫描会识别加密文件头，改为显示“需要密码”及可在当前窗口输入密码重试；密码不会保存到设置或磁盘。其他 7Z 读取失败会给出损坏、分卷或算法兼容性的可读说明，并把底层错误保留为提示详情。
- 压缩包管理默认采用高密度多列浏览；展开某个压缩包时独占一行显示文件树。每个压缩包新增“定位”和“打开”按钮，分别在文件夹中选中或用系统默认程序打开。

## 2.5.14-community.50 — 2026-09-01

- 新增工具箱“压缩包管理”：递归扫描 ZIP、RAR、7Z、TAR/TAR.GZ，直接查看文件树而无需解压。
- 自动识别压缩包内 VPK，读取有效性、内部文件数量与文件名，并按大小写不敏感的 VPK 文件名和当前 addons Mod 对比，区分“已有 Mod”“待导入”和读取失败。
- 支持多选压缩包移动到其他目录，移动前检查同名冲突并提供替换、跳过、取消选项；扫描与 VPK 读取设有条目数和大小上限，降低异常压缩包的资源风险。

## 2.5.14-community.49 — 2026-09-01

- 进一步降低卡片预览的内存和 CPU 峰值：卡片缩略图改为 `512×288` 静态 PNG，GIF 仅解码首帧；详情页仍保留完整原图和动画预览。
- 对卡片预览输入增加 `32 MiB` 文件大小与 `16 MP` 像素上限，避免异常或超大图片在滚动、筛选时造成瞬时内存暴涨；超限内容仅回退为占位图，不影响详情链路。
- 优化缩略图缩放的坐标映射，减少每个像素的重复除法；继续复用可视区域观察、并发队列和 LRU 缓存，快速刷新不再重复读取未进入视口的 VPK。

## 2.5.14-community.48 — 2026-09-01

- 卡片模式改用后端生成的静态缩略图：GIF 只取首帧，缩略图限制在 `640×360` 内；详情窗口仍保留完整原图和动画预览，避免卡片列表同时运行大量 GIF。
- 卡片缩略图与详情原图使用独立的前后端 LRU 缓存，并限制缓存条目和字节数，避免同一张大图在两个链路中重复长期驻留。
- 卡片预览请求增加独立并发队列；卡片刷新、移除或离开可视区域且尚未开始读取时会取消排队任务，降低快速滚动和筛选时的无效 VPK 读取与解码压力。

## 2.5.14-community.47 — 2026-09-01

- 修复服务器列表的异步保存竞态：每次写入会使用独立快照并按顺序提交，快速保存服务器或最近连接记录不会让较早请求覆盖较晚状态；写入失败会恢复内存状态并给出错误。
- 修复服务器编辑窗口的会话归属：保存期间防重复提交，窗口关闭或重新打开后，旧保存结果不会关闭、修改或提示新的表单；编辑和删除会优先使用稳定服务器 ID，避免排序或确认期间误操作其他服务器。
- 保存服务器后重新读取后端规范化配置，确保旧条目的 ID 补全、面板密码脱敏及后端持久化状态会同步到界面。

## 2.5.14-community.46 — 2026-08-31

- 修复面板服务器详情切换/关闭时的子窗口生命周期：地图、难度、上传和 RCON 窗口会同步关闭并废止旧会话，旧请求不会写回新服务器窗口。
- 修复面板服务器重启、地图热重载、VPK 删除与清空地图的确认回调：操作会固定到发起时的服务器，切换服务器或关闭窗口后不会误操作当前新服务器。
- 修复原生文件选择返回后的地图上传路由，以及上传任务和 VPK 风险检测的过期回写；重新打开窗口后按钮状态会正确恢复。

## 2.5.14-community.45 — 2026-08-31

- 修复加载顺序窗口的异步会话竞态：关闭、切换 Mod、刷新、预览或应用规则时，较早返回的读取与预览不再覆盖当前窗口。
- 加固工坊信息存储与 Mod 更新检测的级联保存：阻止并发保存互相覆盖；失败会恢复明确的旧状态；设置已保存后的列表刷新失败不再误报为设置保存失败。
- 修复面板服务器难度、地图与 RCON 窗口在关闭、重开或切换服务器后被旧请求写回、错误提示或关闭当前窗口的问题。

## 2.5.14-community.44 — 2026-08-31

- 修复创意工坊详情页的返回竞态：详情仍在加载时返回列表，会立即废止旧请求，返回结果不再把已经关闭的详情页重新打开。

## 2.5.14-community.43 — 2026-08-31

- 修复 `autoexec.cfg` 编辑器的异步会话竞态：关闭后快速重开时，旧的读取、指令分析、帮助搜索或保存回调不再污染或关闭新窗口。
- 修复模型面数检测窗口的异步生命周期：窗口已关闭时，过期的状态读取不再启动新的扫描，也不会将旧状态回写到重新打开的窗口。

## 2.5.14-community.42 — 2026-08-31

- 发布维护构建：同步本地 EXE、默认版本号与 GitHub Release 资产，确保应用内版本、更新标签和下载包一致。

## 2.5.14-community.41 — 2026-08-31

- 修复设置页和工具箱首屏异步读取的竞态：快速重复打开、刷新或离开页面时，较早返回的请求不再覆盖当前页面，也不会重新挂载过期的控件事件。

## 2.5.14-community.40 — 2026-08-31

- 修复快速连续切换 Mod 目录时，旧目录扫描完成后覆盖新目录列表、提前关闭加载提示或留下旧优先级映射的问题。目录切换现在与刷新共用加载生命周期，并在切换后重新读取 `addonlist.txt` 加载顺序。

## 2.5.14-community.39 — 2026-08-31

- 加固设置页的界面缩放、显示模式、框选和 Ctrl+单击选择设置：异步保存失败时会回滚内存状态、控件和列表显示，避免界面看似成功但重启后恢复旧值。
- 为 addonlist.txt 快照、备份、恢复、删除和融合操作增加防重复执行保护，避免快速点击造成并发写入或过期刷新覆盖。
- 修复设置页 autoexec.cfg 重载后编码、大小、修改时间等元信息不更新，并为独立 autoexec 工具增加关闭/重开与快速搜索的请求竞态保护。

## 2.5.14-community.38 — 2026-08-31

- 修复 MDMP 崩溃转储窗口关闭或快速切换文件后，旧的异步解析结果覆盖当前窗口内容的问题。
- 统一主程序与后端的默认版本号，避免未注入 `-ldflags` 的本地 EXE 显示落后版本。

## 2.5.14-community.37 — 2026-08-31

- 修复设置页“立即触发检测”在更新检测、时间戳保存或列表刷新失败后一直停留在“检测中”的问题；现在无论成功或失败都会恢复按钮。
- 设置页的未记录 Mod 首次插入位置和 addonlist.txt 自动恢复监控开关在异步保存期间会暂时锁定，避免重复点击和过期状态覆盖。

## 2.5.14-community.36 — 2026-08-31

- 修复文件列表刷新请求在扫描进行中被静默丢弃的问题；文件启停、游戏内开关、VPK 工具和 addonlist 操作完成后会等待当前扫描并执行必要的收敛刷新，列表、标签和优先级不再停留在旧状态。
- 加固设置页异步设置链路：保存期间防止重复操作，后端失败时回滚控件状态；修复工坊元数据关闭后“立即检测”按钮仍可用，以及浏览目标、翻译服务失败回滚会再次触发请求的问题。
- 固定 IP 推荐项现在具备与文本输入一致的保存失败处理、状态回滚和按钮禁用保护。

## 2.5.14-community.35 — 2026-09-01

- 修复创意工坊列表在快速切换筛选、排序或类型时，旧请求晚返回污染当前列表的问题。
- 修复创意工坊详情快速切换时，旧详情响应覆盖当前详情的问题。
- 修复面板服务器地图相关窗口点击遮罩关闭时未执行请求失效和定时器清理的问题。

## 2.5.14-community.34 — 2026-08-31

- 统一共享消息弹窗的会话归属：VPK 打包、解包、完整性检测/修复与更新提示不会再由过期异步操作覆盖、关闭或清空后打开的窗口。
- 打包、解包及完整性检测从文件/目录选择开始就防止重复启动；完整性修复期间也会阻止并行检测，避免同一工具重复执行。
- VPK 修复完成后仅在原检测窗口仍有效时展示结果，切换到其他功能后不会突然覆盖正在查看的提示。

## 2.5.14-community.33 — 2026-08-31

- 修复冲突分析窗口在关闭后立即重开时可能一直停留在“检测中”的竞态：旧扫描结果不会写回新窗口，后端扫描锁释放后会为当前窗口自动重新检测。
- 修复创意工坊解析、优选线路轮询和下载任务列表的过期异步回写；关闭/切换页面或实时任务事件到达后，旧请求不再覆盖当前界面，也不会遗留禁用按钮。
- 修复普通服务器和面板服务器详情在快速切换、刷新或关闭时可能显示上一台服务器状态、地图或玩家列表的问题。

## 2.5.14-community.32 — 2026-08-31

- 修复卡片在 Mod 于 `addons`、`disabled` 等目录间移动后仍引用旧路径的问题；详情打开和 Shift/Ctrl 整行选择现在始终使用卡片当前绑定的 Mod。
- 设置页面的后端状态读取改为逐项降级：单个接口短暂失败时会保留上次保存的值并给出可见提示，不再让整个设置页变为空白；同时修复自定义 AI Key 提示重复叠加，以及 autoexec 指令分析/搜索快速输入时的旧异步结果回写。
- 修复异步确认框竞态，旧操作完成后不会关闭或清空后来打开的确认窗口。
- 服务器添加/编辑保存失败时现在会显示具体错误，并保留当前表单内容以便修正后重试。

## 2.5.14-community.31 — 2026-08-31

- 修正上一版对运行中 L4D2 一律禁止写入 `addonlist.txt` 的过度保护：游戏运行时仍可正常编辑，游戏内保存继续由游戏本身作为权威状态。
- 根目录 Mod 在启用前统一校验 `addoninfo.txt` 的 Valve KeyValues 语法；检测到未闭合引号等本体错误时，不再写入会被游戏随后删除的条目，而是提示先使用工具箱修复 VPK。
- 已用本机 `disabled\\AWM Mon3tr.vpk` 复核：99/99 文件校验通过，但原始 `addoninfo.txt` 报 `unexpected EOF`；修复副本可通过完整性检查。普通中文武器 VPK 校验仍通过。

## 2.5.14-community.30 — 2026-08-31

- 修复游戏运行期间修改游戏内开关后，回到 L4D2 保存 Add-ons 又被旧内存列表覆盖的问题：检测到匹配当前游戏目录的 `left4dead2.exe` 正在运行时，程序不再写入 `addonlist.txt`，而是提示先退出游戏再修改。
- 保留自定义目录离线管理能力；只有标准 L4D2 `addons` / `left4dead2` 目录会启用运行中保护，并增加 Windows 进程路径检测与回归测试。
- 已用本机真实进程验证检测结果为 `true`；目标 `【东方Project】魂魄妖梦双持武士刀.vpk` 经完整性检查通过（45/45 文件、`addoninfo.txt` 有效），问题根因确认为游戏运行期间外部写入被保存覆盖。

## 2.5.14-community.29 — 2026-08-31

- 修复文件名自定义标签覆盖普通内容标签的问题：`[三角洲]xxx.vpk` 现在保留自定义主标签，同时继续展示从 VPK 路径识别出的武器、角色、HUD、模型、贴图等标签。
- 自动识别出的主类型在自定义主标签不同的情况下会作为附加标签保留，避免标签筛选信息丢失。
- 已用本机 `[三角洲]波奇赤枭(干草叉).repaired.vpk` 样本复核，能够恢复显示 `武器`、`HUD`、`草叉` 等普通标签。

## 2.5.14-community.28 — 2026-08-31

- 加强卡片模式的游戏内开关视觉提示：绿色、红色、灰色状态改为更明显的整卡底色、边框和侧边色带，便于快速区分。
- 卡片预览改用独立缩略图缓存：GIF 在卡片中只解码首帧，超大图片按卡片尺寸缩放；详情窗口仍保留原始 GIF/高清预览。
- 缩小卡片预取范围并减少不必要的 CSS 全属性过渡，降低大量图片、GIF 和快速刷新/滚动时的布局与绘制压力。

## 2.5.14-community.27 — 2026-08-31

- 改进 VPK `addoninfo.txt` 修复：完整保留可解析的原有字段和嵌套元数据，不再只生成固定模板。
- 对缺失或截断的元数据增加容错恢复；标题、版本、Steam App ID 和描述缺失时从 VPK 文件名补全，描述会明确写入“文件名”。
- 修复结果展示保留字段、补全字段和截断文本恢复情况；原始 VPK 仍始终保留不覆盖。

## 2.5.14-community.26 — 2026-08-31

- 优化 VPK 完整性检测结果弹窗：长路径自动换行，问题列表可滚动，批量结果按文件分组展示。
- Mod 列表右键菜单新增单个“VPK 完整性检测”和多选“检测/修复选中 VPK”。
- 多选检测支持逐项显示结果，并可一次确认修复所有安全可修复项；原 VPK 始终保留不覆盖。

## 2.5.14-community.25 — 2026-08-31

- 工具箱新增“VPK 完整性检测”：校验 VPK 目录、每个文件的 CRC、重复/不安全路径，以及根目录 `addoninfo.txt` 的编码和 Valve KeyValues 语法。
- 对仅存在 `addoninfo.txt` 缺失或语法错误、且其他文件校验通过的 VPK，提供“修复并另存为”；原 VPK 不会被覆盖，修复结果会再次完整校验。
- 对文件损坏、重复路径或不安全路径只提供明确诊断，避免自动修复造成资源丢失。

## 2.5.14-community.24 — 2026-08-31

- 修复首次将根目录 Mod 记录到 `addonlist.txt` 后，被游戏保存操作移除却被误判为“需要回写”的问题：写入前现在会严格校验 VPK 根目录 `addoninfo.txt` 的 Valve KeyValues 语法；格式无效（例如未闭合的描述引号）会明确报错并完全不改写 `addonlist.txt`。
- 移除默认的短时游戏保存回写保护。游戏内保存现在保持权威；只有用户在设置中主动启用的完整 `addonlist.txt` 保护仍会监控并恢复快照。

## 2.5.14-community.23 — 2026-08-31

- 修复游戏已启动后，本程序首次将“未记录”根目录 Mod 写入 addonlist.txt，再由游戏内保存覆盖的问题：本次会话会短暂监测该明确操作；若游戏用旧内存列表保存，会仅把刚启用的 Mod 合并写回，保留游戏对其他 Mod 的修改，并自动创建 `game-save` 历史备份。

## 2.5.14-community.22 — 2026-08-31

- 修复卡片标签悬停无法查看被折叠到“+N”中的完整子标签；标签现在可命中并保留详情卡片点击行为。
- 修复筛选布局切换需等待配置写盘才更新界面的延迟；简洁下拉 / 经典展开会立即应用，并防止快速连续切换时旧保存请求回滚新选择。

## 2.5.14-community.21 — 2026-08-31

- 优化 Mod 卡片预览性能：刷新、筛选、排序时复用未变化的卡片和图片节点，避免反复创建 `<img>`、重新解码和闪烁；仅对可视区域附近卡片按需加载，并降低卡片预览的加载优先级。
- 为预览源增加稳定签名：同一 Mod 在 `addons` 与 `disabled` 之间移动时复用已加载图片；VPK 或外部同名图片实际变化时自动失效，避免显示旧预览。
- 筛选布局设置改为更清晰的“简洁下拉 / 经典展开”彩色选择卡；经典布局的位置、游戏开关和内容预设按语义区分颜色，窄屏下自动适配。
- 修复筛选布局保存失败后仅回滚界面、未回滚前端配置缓存的问题，避免后续设置保存再次写回失败的布局。
- 搜索与异步筛选增加防抖、请求序号和渲染代次保护，快速操作时不再让旧结果覆盖当前列表。

## 2.5.14-community.20 — 2026-08-30

- 优化卡片视图刷新：按 Mod 路径与实际展示数据复用未变化的卡片，刷新结果和排序都不变时不再替换整个卡片 DOM；已加载的预览图片节点会保留，避免重新解码和闪烁。
- 卡片预览继续使用懒加载和受限并发队列，并在滚动至可视区域前预取；屏幕外卡片启用 Chromium 的 `content-visibility` 隔离，降低大型 Mod 库刷新、滚动时的布局与绘制开销。
- 修复无预览图卡片输出空 `src` 属性的问题，避免浏览器将当前页面误当作图片资源请求。

## 2.5.14-community.19 — 2026-08-30

- 修复 addonlist.txt 在游戏启动/退出期间短暂不可读或写入不完整时，刷新会把已知游戏内开关误清成“未记录”的问题；现在会保留上一次成功读取的状态，并记录诊断日志。
- 保留 VPK 重新解析时已有的游戏内开关状态，避免临时文件变化与 addonlist.txt 读取窗口叠加造成状态回退。
- 修复首次记录根目录 Mod 时把文件名统一转成小写的问题；比较仍不区分大小写，但写回 addonlist.txt 会保留磁盘上的真实大小写和中文名称。
- 对扩展名为 `.vpk` 但实际为 ZIP 的文件给出明确提示（例如 `3558049615.vpk`），指出需要重新下载、解压或改正文件，而不是只显示魔数错误。
- 增加 GBK 根目录文件名、缺失/损坏 addonlist 和 ZIP 伪装 VPK 的回归测试。

## 2.5.14-community.18 — 2026-08-30

- Mod 列表工具栏新增“优先级 # 定位”：输入 addonlist.txt 的编号即可在任意当前排序方式下滚动并高亮对应 Mod；若筛选暂时隐藏目标会明确提示，不会擅自清空筛选。
- “未记录”Mod 首次在游戏内开启时的位置可在“设置 → addonlist.txt 生命周期管理”中选择：列表顶部、紧跟最后一个已开启 Mod 或列表末尾。默认保持列表末尾，不改变已有 Mod 的相对顺序；启用成功会提示实际优先级编号。
- 新增配置持久化与 GBK/ANSI、BOM、CRLF 保真测试，确保上述插入位置不会破坏游戏原有 addonlist.txt 编码或格式。

## 2.5.14-community.17 — 2026-08-30

- 审计设置页的 Ctrl/Shift 选择、框选、界面缩放、工坊选项、更新检测、翻译服务和 addonlist 监控链路，修复快速连续修改设置时异步保存乱序导致的状态回退。
- `SaveAppConfig` 现在合并保存工坊网络、浏览器、更新检测、翻译服务、Mod 轮换和 addonlist 监控设置，并保护已加密的自定义 API Key 不被“已设置”占位文本覆盖。
- 配置写盘统一串行化，启动与重启后设置状态保持一致；设置页同步 addonlist 监控缓存，避免后续保存其他设置恢复旧开关。

## 2.5.14-community.16 — 2026-08-30

- 修复 Mod 列表复选框点击后看似无反应的问题：移除会回滚浏览器勾选状态的默认行为拦截，并保留 Shift/Ctrl 多选与列表/卡片模式同步。
- 完成文件移动冲突处理、冲突 Mod 优先级联动和相关列表交互修复，发布 Windows amd64 安装包。

## 2.5.14-community.15 — 2026-08-29

- 冲突分析会按实际加载优先级排序并显示优先级；可将选中的冲突 Mod 一键紧邻移到锚点 Mod 前面或后面，并修复调整游戏内开关后分析界面偶发卡住的问题。
- 文件列表补齐 Shift 连续选择与 Ctrl 多选，优化主体、标签和优先级信息的自适应排版，并修复刷新后新启用根目录 Mod 未显示优先级的问题。
- 新增基于 VPK 实际资源路径的主体识别：区分地图、武器、道具、角色语音等内容；语音包以标准角色语音目录为准，避免由台词文件名误判角色。
- 新增 xdReanimsBase（XDR）动画识别：显示角色/模型、slot、可识别动作与证据；同一动作冲突时明确提示 slot 数字较小者优先，基础包不会虚构具体角色或 slot。

## 2.5.14-community.14 — 2026-08-29

- 审计并修复文件列表批量启用/禁用后的状态徽章、游戏内开关按钮不同步问题。
- 修复批量隐藏/取消隐藏将未变化文件错误计入成功数量的问题，通知现在区分成功、跳过和失败。
- 防止工坊下载按钮、分组下载和单项下载重复点击创建重复任务；优选 IP 期间关闭页面会取消轮询。
- 修复下载任务快速双击“重试”可能启动多个下载协程的竞态。

## 2.5.14-community.13 — 2026-08-27

- 新增 `autoexec.cfg` 工具箱：可从当前游戏路径推断并编辑配置，实时识别已使用的控制台指令、别名、风险和来源，并提供 L4D2/L4N 指令手册。
- 根据实机文件编码保留 `autoexec.cfg` 与 `addonlist.txt` 的 UTF-8、GBK/ANSI、Windows-1252、UTF-16、BOM 和换行格式；写入前保留原始字节备份。
- 修复 VPK 中 GBK 文件名及 GBK/Windows-1252 `addoninfo.txt`、`missions/*.txt` 的解析；VPK 打包和解包也统一处理非 UTF-8 名称。
- 改善 `addonlist.txt` 生命周期管理界面，并修复 Windows 下替换已有配置文件的可靠性。

## 2.5.14-community.9 — 2026-08-26

- `addonlist.txt` 的“监控并自动恢复”现在默认关闭；仅在用户手动开启后才会持续监视、持久化该选择并在下次启动时恢复监控。创意工坊 Mod 复制到根目录不再擅自开启监控。
- 修复加载顺序优化窗口被通用模态框宽度限制的问题。预设按钮会根据弹窗实际可用空间自动换列；窄窗口中规则区与预览改为上下排列，大窗口仍保持完整双栏工作区。
- 同步直接构建使用的 Community Fork 默认版本号，避免本地构建显示过期版本。

## 2.5.14-community.8 — 2026-08-26

- 重构 `addonlist.txt` 加载顺序工作台：扩大窗口并改为预设/规则与实时预览分栏布局，窄窗口自动切换为单列。
- 新增一键“推荐：目录规整”“根目录优先”“工坊聚集”“开启项优先”“关闭项优先”规则；所有规则均稳定重排，绝不改写 Mod 的游戏内开关值。
- 细化手动策略：可在自动规则中选择保持原顺序、开启项优先或关闭项优先；锚点前后关系仍在最后应用并覆盖自动排序。

## 2.5.14-community.7 — 2026-08-26

- 扩展内容预设：新增八位幸存者、八种特殊感染者、普通/罕见感染者、地图游戏模式，以及模型/贴图/脚本、可搬运物与环境容器等筛选入口。
- 角色与感染者预设显示中文名称和稳定的英文内部标签；可一键查看聚合标签，或展开后精确多选。
- 基于本机 `addons` 目录中 1,760 个 VPK 的只读目录索引审计确认预设标签有真实样本，并新增角色、感染者与地图模式标签回归测试。

## 2.5.14-community.6 — 2026-08-26

- 修复 Release 工作流对 `gh release view` 非标准退出码的误判，改由最终发布命令处理 Release 唯一性。

## 2.5.14-community.5 — 2026-08-26

- 修复 GitHub Actions 在 tag 检出环境下误判发布身份的问题，改为校验仓库、操作者和语义化 tag 事件上下文。

## 2.5.14-community.4 — 2026-08-26

- 修复 Release 工作流对本地 Git 身份的重复、脆弱校验；CI 现在可靠地验证带注释的 tag 与实际构建提交一致。

## 2.5.14-community.3 — 2026-08-26

- Mod 详情页中的创意工坊 Mod 现在可直接在默认浏览器打开对应的 Workshop 页面。
- 增加按当前筛选范围执行的 Mod 文件冲突分析，并在详情中展示按风险等级归类的冲突项。
- 修复本地直接构建的 EXE 被标记为 `0.0.0-dev` 时，首次启动误报可更新至旧正式版的问题；开发构建会跳过更新检查。

## 2.5.14-community.2 — 2026-08-26

- 应用内更新源默认固定为 `zombrain69/lytvpk`，开发构建也不会再因漏传构建参数而回退到上游仓库。
- “关于”页同时保留本 Fork 仓库与上游项目入口：前者用于更新、源码和反馈，后者仅用于出处与致谢。
- Windows 程序与更新包统一使用 `LytVPK-Community-Fork.exe`，更新器拒绝包含其他 EXE 名称的 ZIP。

## 2.5.14-community.1 — 2026-08-26

本版本基于上游 `LaoYutang/lytvpk` 提交 `c977064445c46ce4c69d1b88c038bb95c9240c9f`，由 `zombrain69/lytvpk` 作为非官方 Community Fork 发布。

### 主要修改

- 完善 `addonlist.txt` 的保存、备份、恢复、删除和运行时覆盖恢复生命周期。
- 增加 addon 加载顺序预览、工坊/根目录分组和显式前后约束。
- 增加归档内容标签、重复扫描检测和 VPK 模型统计能力。
- 增加单实例启动辅助逻辑及相关前端绑定。
- 保留 GPL-3.0-only、上游版权和第三方许可证说明。

### 发布说明

- 这是 `LaoYutang/lytvpk` 的修改版，不代表原作者官方发布。
- 正式版本应使用语义化版本 tag，并从同一 tag 提供完整源码和 Windows 构建产物。
- 应用内更新源仅应指向 `zombrain69/lytvpk` 的 Release，不应指向上游仓库。
